# Core UI
streamlit>=1.30

# LIVE Kotak Neo data (enabled). If the Streamlit Cloud build fails on this
# line, comment it back out (add "# " in front) and run the app locally for
# live data instead — see README.
neo_api_client @ git+https://github.com/Kotak-Neo/Kotak-neo-api-v2.git@v2.0.2
