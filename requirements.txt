# Core UI
streamlit>=1.30

# LIVE Kotak Neo data.
# IMPORTANT: this only builds on Python <= 3.12. On Streamlit Cloud you MUST
# pin the Python version to 3.12 (App → Settings → Advanced → Python version).
# If left on the default (3.14) the build fails and the whole app errors out.
neo_api_client @ git+https://github.com/Kotak-Neo/Kotak-neo-api-v2.git@v2.0.2
